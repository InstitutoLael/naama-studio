# Instagram API Token
# Generate a long-lived User Token from the Facebook Developer Portal > Instagram Basic Display
# If left empty, the site will show the curated fallback grid (static images).
VITE_INSTAGRAM_TOKEN=
